apiVersion: apps/v1
kind: Deployment
metadata:
  name: sync-service
  namespace: foculist
spec:
  replicas: 2
  selector:
    matchLabels:
      app: sync-service
  template:
    metadata:
      labels:
        app: sync-service
    spec:
      initContainers:
        - name: otel-javaagent
          image: ghcr.io/open-telemetry/opentelemetry-operator/autoinstrumentation-java:1.33.6
          command: ["cp", "/javaagent.jar", "/otel/opentelemetry-javaagent.jar"]
          volumeMounts:
            - name: otel-javaagent
              mountPath: /otel
      containers:
        - name: app
          image: yourorg/sync-service:latest
          ports:
            - containerPort: 8086
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "prod"
          envFrom:
            - configMapRef:
                name: foculist-runtime
            - secretRef:
                name: foculist-runtime
          volumeMounts:
            - name: otel-javaagent
              mountPath: /otel
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8086
            initialDelaySeconds: 20
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8086
            initialDelaySeconds: 10
            periodSeconds: 10
      volumes:
        - name: otel-javaagent
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: sync-service
  namespace: foculist
spec:
  selector:
    app: sync-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8086
  type: ClusterIP
