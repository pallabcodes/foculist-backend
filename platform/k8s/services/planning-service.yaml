apiVersion: apps/v1
kind: Deployment
metadata:
  name: planning-service
  namespace: foculist
spec:
  replicas: 2
  selector:
    matchLabels:
      app: planning-service
  template:
    metadata:
      labels:
        app: planning-service
    spec:
      containers:
        - name: app
          image: yourorg/planning-service:latest
          ports:
            - containerPort: 8083
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "prod"
          envFrom:
            - configMapRef:
                name: foculist-runtime
            - secretRef:
                name: foculist-runtime
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8083
            initialDelaySeconds: 20
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8083
            initialDelaySeconds: 10
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: planning-service
  namespace: foculist
spec:
  selector:
    app: planning-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8083
  type: ClusterIP
