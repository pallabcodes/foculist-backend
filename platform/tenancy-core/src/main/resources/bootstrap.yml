spring:
  cloud:
    aws:
      region:
        static: ${AWS_REGION:us-east-1}
      endpoint: ${AWS_ENDPOINT:http://localhost:4566}
      credentials:
        access-key: test
        secret-key: test
      secretsmanager:
        enabled: true
        fail-fast: false
        regions:
          - ${spring.cloud.aws.region.static}
        prefixes:
          - /foculist/common/
