plugins {
    `java-library`
    id("io.spring.dependency-management")
}

description = "Shared multi-tenancy core for foculist services"

// Dependencies are managed centrally in the root build.gradle.kts
dependencies {
}
